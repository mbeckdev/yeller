// !!!!!!!!!!!!!!!!!!!!anys still in here!!!!!!!!!!!!!!!!!!!!
// !!!!!!!!! put a type on the RouteWithSubRoutes function!!!!!!!!!!!!!!!!!!!!

// import React from 'react';
// import { Route } from 'react-router-dom';

// type RouteWithSubRoutesProps = {
//   path: string;
//   component: React.FC;
//   routes: any;
// };

// // const RouteWithSubRoutes: React.FC = (route: RouteWithSubRoutesProps) => {
// function RouteWithSubRoutes(route: RouteWithSubRoutesProps) {
//   return (
//     <Route
//       path={route.path}
//       element={(props: any) => (
//         // pass the sub-routes down to keep nesting
//         <route.component {...props} routes={route.routes} />
//       )}
//     />
//   );
// }

// export default RouteWithSubRoutes;

// element={(props) => <route.component {...props} routes={route.routes} />}
// render={(props) => <route.component {...props} routes={route.routes} />}
// return <Route path={route.path} element={route.component} />;

export {};
