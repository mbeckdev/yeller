import routeConstants from 'shared/constants/routes';
import Notifications from '.';

const thingToExport = {
  path: routeConstants.NOTIFICATIONS.route,
  component: Notifications,
};

export default thingToExport;
